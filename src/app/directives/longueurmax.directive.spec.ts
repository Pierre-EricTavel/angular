import { LongueurmaxDirective } from './longueurmax.directive';

describe('LongueurmaxDirective', () => {
  it('should create an instance', () => {
    const directive = new LongueurmaxDirective();
    expect(directive).toBeTruthy();
  });
});
